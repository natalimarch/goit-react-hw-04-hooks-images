
export const fields = {
    query: {
        type: "text",
        autoComplete: "off",
        autoFocus: true,
        placeholder: "Search images and photos",
        name: "query",
        required: true
    }
}
